# Discord 봇용 Fly 설정 (웹 서비스 아님 → http_service 없음, 자동정지 없음)
app = 'ye-worktime-bot'
primary_region = 'nrt'

[build]

[env]
  DATA_DIR = '/data'

[[mounts]]
  source = 'botdata'
  destination = '/data'

[[vm]]
  size = 'shared-cpu-1x'
  memory = '256mb'
  cpus = 1
  memory_mb = 256
