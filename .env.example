# 이 파일을 복사해서 이름을 .env 로 바꾼 뒤 값을 채우세요.
# .env 는 절대 GitHub에 올리지 마세요 (.gitignore 에 포함돼 있습니다).

# 디스코드 개발자 포털 → Bot → Reset Token 으로 발급받은 토큰
DISCORD_TOKEN=여기에_봇_토큰_붙여넣기

# 정산 결과를 올릴 텍스트 채널 ID (비우거나 0 이면 자동으로 아무 채널)
REPORT_CHANNEL_ID=0

# SQLite 저장 경로. 클라우드 배포 시 영구 볼륨 경로(예: /data)
# DATA_DIR=/data
